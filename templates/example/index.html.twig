{% extends 'base.html.twig' %}

{% block title %}Finder{% endblock %}

{% block body %}

<div class="example-wrapper">
	{% if files %}
		<ul>
			{% for file in files %}
				<li>{{ file }}</li>
			{% endfor %}
		</ul>
	{% endif %}
</div>

{% endblock %}
